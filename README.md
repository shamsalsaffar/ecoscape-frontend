# how to use

1. clone the project
2. open the project in VSCode
3. open the terminal and <ins>run npm install</ins>
4. start the project with <ins>npm run dev</ins>
5. go to the localhost where your application is running
